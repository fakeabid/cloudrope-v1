# In development, Vite proxies /auth and /files to localhost:8000 automatically.
# Only set this for production deployments where the frontend is served separately.
# VITE_API_BASE=https://your-backend.example.com
